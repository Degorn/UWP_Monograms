<UserControl
    x:Class="UWP_Monograms.Components.Conditions.ConditionCellControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_Monograms.Components.Conditions"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <Style x:Key="ConditionBorderStyle" TargetType="Border">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="LightGray" />
            <Setter Property="Padding" Value="4" />
        </Style>
        <Style x:Key="ConditionTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource ConditionBorderStyle}">
        <TextBlock Style="{StaticResource ConditionTextBlockStyle}"
                   Text="{x:Bind Model.Value, Mode=OneWay}"/>
    </Border>
</UserControl>
