<UserControl
    x:Class="UWP_Monograms.Components.Conditions.ConditionPackControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_Monograms.Components.Conditions"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MonogramsLib.Models"
    xmlns:viewModels="using:UWP_Monograms.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <ItemsPanelTemplate x:Name="VerticalStackPanel">
            <StackPanel Orientation="Vertical"/>
        </ItemsPanelTemplate>
        <ItemsPanelTemplate x:Name="HorizontalStackPanel">
            <StackPanel Orientation="Horizontal"/>
        </ItemsPanelTemplate>

        <DataTemplate x:Name="Item" x:DataType="models:ConditionItem">
            <local:ConditionCellControl Model="{x:Bind}"/>
        </DataTemplate>
    </UserControl.Resources>

    <ItemsControl x:Name="Isc" ItemsSource="{x:Bind Model.Conditions}"
                  ItemsPanel="{x:Bind ItemsPanelTemplate, Mode=OneWay}"
                  ItemTemplate="{StaticResource Item}">
</ItemsControl>
</UserControl>
